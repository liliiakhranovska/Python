import bishop
import rook


def try_to_move(current_point, target_point, board):
    if bishop.try_to_move (current_point, target_point, board) is True or bishop.try_to_move (current_point, target_point, board) is True:
        return True
